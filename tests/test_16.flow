fun suma(x:int, y:int): int {
    return x + y;
}


fun print_neg(x: int): void {
    if (x > 0 != false) {
        print(x);
    }
}

print(suma(2, 4));
print_neg(suma(-4, 2));

# Resultado
#
# 6
# -2
